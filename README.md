# How to contribute

1. Clone the code:

    > git clone \<url>

2. For each add, modify or delete in files, run multi-browser package again:

    > python .\package.py

3. Import from specific browser in 'dist' folder or reload the extension/addon on browser

Made with 💚 and 🤖